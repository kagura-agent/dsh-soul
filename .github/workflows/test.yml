name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Syntax check
        run: |
          node --check src/index.js
          node --check src/client.js
      - name: Client static consistency (i18n keys + helper references)
        run: node .selftest-client.mjs
      - name: Host route integration tests
        run: node .selftest-e2e.mjs
